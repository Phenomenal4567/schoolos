<?php

namespace Tests;

use Illuminate\Foundation\Testing\TestCase as BaseTestCase;

/**
 * Standard Laravel base test case. This suite assumes the rest of the
 * Laravel skeleton (composer.json, bootstrap/app.php, phpunit.xml, config/)
 * exists alongside this Phase 1 scaffolding pass — only the service-layer
 * and its tests were in scope for this pass.
 */
abstract class TestCase extends BaseTestCase
{
    //
}
